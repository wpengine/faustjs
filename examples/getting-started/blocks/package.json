{
  "name": "@getting-started/blocks",
  "version": "1.0.0",
  "private": false,
  "description": "Headless block components",
  "repository": {
    "url": "wpengine/headless-framework",
    "directory": "blocks"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -d -p tsconfig.json",
    "dev": "tsc -d -w -p tsconfig.json",
    "prepare": "npm run build"
  },
  "dependencies": {
    "typescript": "4.2.2"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*"
  }
}
