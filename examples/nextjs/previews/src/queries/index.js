import { queries as single } from '../wp-templates/single';
import { queries as page } from '../wp-templates/page';
import { queries as archive } from '../wp-templates/archive';

export default { single, page, archive };
