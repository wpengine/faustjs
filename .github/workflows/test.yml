name: test
on: pull_request
jobs:
  packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: NPM Install
        run: npm install
        env:
          CI: TRUE
      - name: Run tests
        run:  npm run test
        continue-on-error: FALSE
